# Overview

These docs provide a generic frontend convention baseline across projects.

- Runtime-neutral
- Package-manager-neutral
- Compatible with TypeScript-first React projects

Use project `AGENTS.md` as source of truth when present. Use this docs set as fallback reference.

Example and pattern snippets in this skill are illustrative. They must not override project-local scaffolding templates/snippets.
