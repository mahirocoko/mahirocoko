# Reference Excerpts

These are small skill-local examples for practicing reference anatomy. They are **non-canonical evidence**, not design prompt assets.

Use them to test or explain how `frontend-design brief` separates structure from style leakage:

- keep section jobs, type roles, media roles, motion purpose, responsive constraints
- reject copied liquid glass, pill navigation, cinematic dark SaaS defaults, hover-scale decoration, and generic glow/gradient systems unless required by the product

For real projects, pass a workspace-local handoff or reference file with `--handoff <path>` or `--reference <path>`.
