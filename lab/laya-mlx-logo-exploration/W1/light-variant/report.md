# W1 Light-Surface Variant

One native, source-referenced image generation edit was run using only `W1/raw.png`. The returned 2172×724 PNG preserves the horizontal logo lockup, dark navy/graphite surfaces, orange accent, and exact “Laya MLX Lab” wording closely.

The provider result is RGB with a checkerboard pattern baked into the pixels; it has no alpha channel, so the requested genuinely transparent background is blocked. The exact provider PNG bytes are preserved at `W1/light-variant/raw.png`, with the prompt and full receipt alongside it. No second image generation call was made, and `W1/raw.png` was not modified.
