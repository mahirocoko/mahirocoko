# A1 large composition adaptation

Use `../A1/raw.png` as the direct image reference for the exact selected character. Execute exactly one native image-generation reference edit. Preserve the identity lock and change only the assigned composition/scale axis. Save the exact prompt before the call, preserve the first returned raster byte-for-byte, and record provider/result identity, dimensions, mode, source hash, output hash, and call count. No cleanup, crop, recolor, vectorization, reviewer, subagent, schedule, or product-repo edit. This remains a raw Adapt candidate until Mahiro accepts it.
