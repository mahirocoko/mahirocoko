Read `brief.md` in this folder completely. You are the native Codex image-generation visual owner for six Laya MLX Lab raw logo/identity concepts. Work only in this personal lab folder; the product repository is read-only context. Do not edit any product code, execute installs, commit, push, or deploy. No nested subagents, reviewers, or scheduled follow-ups.

Make exactly six independent native `image_generation` calls, sequentially: W1/W2 wordmark-led, M1/M2 letterform or monogram plus wordmark, C1/C2 mascot plus wordmark. Use GPT Image 2 if the native tool exposes it; report the actual provider/model identity rather than guessing. Author a distinct exact prompt for each concept from the brief and persist it as UTF-8 before submitting that concept. One output per call; preserve every first return untouched. Do not request a logo based on one of the earlier outputs, and do not generate a webpage or full mockup. Keep one legible isolated mark per canvas, enough dark background context to assess contrast, and no invented scientific claims. The two character concepts should be independent identities, not a shared mascot iterated twice. Generated lettering is raw concept evidence; any typo should be recorded, never painted over.

For each call, record label, exact submitted prompt path, tool/provider receipt or returned artifact identity, call number, raw native path, dimensions, color mode, and SHA-256. Copy only the exact provider-returned file to a label-specific folder, preserving bytes. Never choose by searching a global output folder for its newest file. On a returned failure, record it and do not retry or use scripts as a fake imagegen substitute. If the imagegen tool is unavailable, stop and report that blocker with zero calls.

After all six calls (or a clear terminal blocker), produce a short `report.md` with the receipt table and observed mechanical findings at full and small size. Make one contact sheet only as a derivative for review; do not filter, crop, retouch, upscale, vectorize, or declare a winner. Mahiro will choose a direction before any production export or blog integration. Stop after writing the report.
