interface:
  display_name: "Generate 2D Map"
  short_description: "Choose and build 2D game maps."
  default_prompt: "Use $generate2dmap to choose the right 2D map strategy and build the map assets, collision data, and game integration."

policy:
  allow_implicit_invocation: true
