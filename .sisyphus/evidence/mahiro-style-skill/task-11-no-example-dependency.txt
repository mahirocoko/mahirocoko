Task 11 evidence: canonical path has no `examples/` dependency

Command intent:
- Verify that `SKILL.md`, `README.md`, `foundations/`, and `patterns/` do not reference `examples/`.

Environment note:
- `rg` is not installed in this worktree shell, so I ran an inline Python regex scan with the same pattern and target paths.

Pattern:
examples/

Paths:
- `.agents/skills/mahiro-style/SKILL.md`
- `.agents/skills/mahiro-style/README.md`
- `.agents/skills/mahiro-style/foundations`
- `.agents/skills/mahiro-style/patterns`

Output:
```
[no matches]
```

Interpretation:
- The canonical reading path is self-contained for Task 11 scope.
- Remaining legacy `examples/*` docs still exist as preserved history, but canonical docs do not route readers through them.
